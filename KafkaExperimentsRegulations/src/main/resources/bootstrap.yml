spring.sleuth.sampler.probability: 1.0
spring.zipkin:
  base-url: ${ZIPKIN_URL}
  sender.type: kafka
  locator.discovery.enabled: true
spring.kafka.producer.bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}
server.port: ${REGULATIONS_SERVICE_PORT}
spring.application.name: kafka-experiments-regulations
spring:
  cloud:
    config:
      uri=${KITE_EXPERIMENTS_CONFIG_SERVER}
